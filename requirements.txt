fastembed>=0.3.0
chromadb>=0.5.0
pymupdf>=1.24.0
docling>=2.0.0
# CPU-only torch — install before docling to avoid pulling CUDA (~1.5 GB extra):
#   uv pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   uv pip install -r requirements.txt
